# NekoraChat Environment Variables

# Database path (default: file:./data/app.db)
DATABASE_URL=file:./data/app.db

# 说明：
# - 访问密码、JWT 密钥、加密密钥均由系统在首次启动时自动生成并持久化到数据库，
#   无需再通过环境变量配置。
# - 初始访问密码会输出到启动日志中，请注意查看并妥善保存，登录后可在「设置 - 通用」中修改。
