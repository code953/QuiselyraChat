services:
  nekorachat:
    build: .
    image: nekorachat:local
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      DATABASE_URL: ${DATABASE_URL:-file:./data/app.db}
    restart: unless-stopped
